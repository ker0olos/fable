name: anilist

on:
  workflow_dispatch:
  schedule:
    # Once a week "At 00:00 on Sunday" (see https://crontab.guru/once-a-week)
    - cron: "0 0 * * 0"

jobs:
  update-last-page-cache:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ssh-key: ${{ secrets.SSH_PRIVATE_KEY }}
      - uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x
      - run: deno run --allow-read --allow-write --allow-net packs/anilist/pool.ts
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v4
        with:
          title: 'Updated "anilist/pool.json"'
          commit-message: '[bot] Updated "anilist/pool.json"'
          branch: update-anilist-pool
          delete-branch: true
          body: |
            This PR is auto-generated

            Updated the pool cache for each possible gacha query

            (See #9)
            (See #45)
          labels: |
            anilist
