{
  "$id": "https://fable.deno.dev/schema.builtin.json",
  "$comment": "Only used for the builtin packs. Those properties don't work on normal community packs.",
  "title": "builtin.manifest",
  "allOf": [{ "$ref": "schema.json" }],
  "required": ["type", "author"],
  "properties": {
    "type": {
      "type": "string",
      "pattern": "builtin"
    },
    "commands": {
      "type": "object",
      "description": "Register new slash commands",
      "additionalProperties": false,
      "patternProperties": {
        "^.*$": {
          "type": "object",
          "required": ["source", "description"],
          "additionalProperties": false,
          "properties": {
            "source": {
              "type": "string"
            },
            "description": {
              "type": "string"
            },
            "options": {
              "type": "array",
              "items": {
                "type": "object",
                "required": ["id", "description", "type"],
                "additionalProperties": false,
                "properties": {
                  "id": {
                    "type": "string"
                  },
                  "description": {
                    "type": "string"
                  },
                  "type": {
                    "type": "string",
                    "enum": ["string", "integer", "boolean"]
                  },
                  "required": {
                    "type": "boolean"
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
