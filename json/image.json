{
  "$id": "https://raw.githubusercontent.com/ker0olos/fable/main/json/image.json",
  "type": "array",
  "title": "image",
  "items": {
    "title": "image",
    "type": "object",
    "required": ["url"],
    "additionalProperties": false,
    "properties": {
      "url": {
        "title": "url",
        "type": "string",
        "description": "A url of the image (Fable forces the aspect-ratio of all images to 230:325) (recommended size: 450x635)"
      },
      "artist": {
        "title": "artist",
        "description": "The artist responsible for the image",
        "required": ["username"],
        "additionalProperties": false,
        "properties": {
          "username": {
            "title": "username",
            "type": "string",
            "description": "The username of the artist"
          },
          "url": {
            "title": "url",
            "type": "string",
            "description": "A url leading to the artist's page"
          }
        }
      }
    }
  }
}
